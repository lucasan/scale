<section class="item-list">
  {% if posts %}
    <ul>
    {% for post in posts %}
      <li>
        <a href="{{ path('scale.content', {'id': post.id}) }}">{{ post.title }} - ({{ post.user.name }})</a>
      </li>
    {% endfor %}
    </ul>
  {% endif %}
</section>
